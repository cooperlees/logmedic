{{- if and .Values.secrets.create (not .Values.secrets.name) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "logmedic.fullname" . }}
  labels:
    {{- include "logmedic.labels" . | nindent 4 }}
type: Opaque
stringData:
  ANTHROPIC_API_KEY: {{ .Values.secrets.anthropicApiKey | quote }}
  GITHUB_TOKEN: {{ .Values.secrets.githubToken | quote }}
{{- end }}
